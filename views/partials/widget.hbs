{{!-- NeurowexTech AI Chat Widget
     Included once in views/layouts/main.hbs, just before </body>.
     The script fetches /api/widget-data lazily (on first open) for live DB content,
     and falls back to built-in data automatically if the endpoint is unavailable.
--}}
<script src="/js/widget.js" defer></script>
